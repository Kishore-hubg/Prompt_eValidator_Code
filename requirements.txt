fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0
pydantic>=2.0
sqlalchemy>=2.0
python-docx>=1.1.0
pymongo>=4.8
PyJWT[crypto]>=2.10
httpx>=0.27
# Teams bot packages — only needed for teams_bot/ service (not Vercel)
# Install locally with: pip install botbuilder-core botbuilder-integration-aiohttp aiohttp
